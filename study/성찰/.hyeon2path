이 파일은 PATH SKIP 이 발생해서 추가하는 파일이다.

GIT은 파일 단위로 자료를 인식하고 처리한다.
따라서 디렉토리 내에 파일이 없으면 PATH SKIP 된다.
파일이 존재하는 하위 디렉토리까지 쭉 스킵된다.
- 디렉토리 하위로 폴더가 1억개 있어도, 파일이 전혀 없으면 add 조차 되지 않는다.

	ex1) "성찰" 디렉토리에는 파일이 없고, 하위 디렉토리만 존재
		1. "성찰" -> "내가 틀릴수도 있습니다" 에는 파일이 존재.
		2. "성찰 / 내가 틀릴수도 있습니다" 로 PATH SKIP 됨.
		=> 2번이 루트 디렉토리 처럼 열리고, 파일이 존재하게 됨.

	ex2) "TEST" 디렉토리를 생성했지만, 파일은 전혀 없음
		=> git add TEST 시 무반응. GIT은 인식 안함.


지금처럼 필요한 부분을 구상하고, 배우고, 적용하고, 정리하면서 꾸준히 나아가자.